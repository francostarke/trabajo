#include <stdio.h>

main(){
	int c,e,tabs,l;
	espacios=0;
	tabs=0;
	lineas=0;
	while ((c=getchar())!=EOF){
		if (c==' ')
			e=e+1;
		if (c=='\t')
			tabs=tabs+1;
		if (c=='\n')
			l=l+1;
	}
	printf ("Espacios: %d. Tabs: %d. Líneas: %d.\n",espacios,tabs,lineas);
}
